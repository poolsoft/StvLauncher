<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:background="#889900"
    android:gravity="center"
    android:orientation="vertical">

    <ProgressBar
        android:id="@+id/progressbar"
        android:layout_width="@dimen/progressbar_size"
        android:layout_height="@dimen/progressbar_size"
        android:indeterminate="true"
        android:indeterminateDrawable="@drawable/progress_image"
        android:indeterminateDuration="2000" />

    <TextView
        android:id="@+id/loading_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/workspace_loading_tips_marginTop"
        android:focusable="false"
        android:focusableInTouchMode="false"
        android:text="@string/app_workspace_loading_text"
        android:textColor="@color/white"
        android:textSize="@dimen/workspace_loading_tips_textSize" />

</LinearLayout>